用户认证系统页面中密码修改功能缺少成功反馈：
1. ChangePasswordForm 组件的 handleSubmit 中，当 changePassword 返回 result.success 为 true 时，组件没有任何用户可见的成功提示就直接因为 logout 触发的状态变更跳转到了"请先登录"界面。用户只能在页面上看到登录表单，完全不知道密码修改已成功。需要在提交成功后展示反馈信息（如"密码修改成功，请重新登录"），确保用户在看到登录表单之前已经收到确认。
2. 修复完成后重新运行 pnpm run test，需要让 auth 相关的所有测试通过。
