你实现的任务看板页面还存在一些问题，需要进行修复：
1. 拖拽排序功能存在 BUG：dragOverTaskIndex 状态变量始终为 null，因为 handleTaskDragOver 回调是空函数体没有将悬停位置上报，同时 KanbanColumn 中的 data-task-index 属性从未被读取用于计算目标位置。导致所有任务被拖拽后只能排在末尾，无法插入到列的中间位置。需要修复拖拽悬停时的位置上报和目标索引计算逻辑。
2. 新增任务弹窗有状态残留问题：当用户在同一列点击"新增任务"填写内容后关闭弹窗，再次点击同一列的"新增任务"时，之前填写的表单内容会残留，用户期望看到的是空表单。原因是 TaskModal 的 key 属性只区分了"编辑哪个任务"和"新增到哪列"，没有覆盖"同一列多次新增"的场景。
3. utils.js 中的 sortTasksByPriority 函数被导出且测试文件编写了测试用例，但在 KanbanPage 中未被导入或调用，任务的优先级排序完全依赖 groupTasksByColumn 中的 order 字段。需要清理死代码或将排序函数集成到看板列渲染流程中。
4. 修复完成后重新运行 pnpm run test，需要让 kanban 相关的所有测试通过。
